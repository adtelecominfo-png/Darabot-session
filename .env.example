# Port the server runs on (Render sets this automatically)
PORT=3000

# The admin console is hardcoded at /unknownofrun.
# Password: Akin$ola@2020

# Render PostgreSQL internal connection string for persistent admin settings
DATABASE_URL=postgresql://user:password@host:5432/database
# Set to disable only for a local PostgreSQL instance without SSL
DATABASE_SSL=disable
